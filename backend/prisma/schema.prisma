generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:dev.db"
}

model User {
  id        String   @id
  name      String   @unique
  pass      String
  coins     Int
  tickets   Int
  createdAt DateTime @default(now())
}
